<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <script>
        //------TIPOS PRIMITIVOS---------
// 1. String - texto, sempre entre aspas
// passo a passo/*// 
//"let" para valores que vao mudar      
 let nomeAluno = "Maria";       
 let idade = 20;        idade = 21; 
//posso alterar depois    

//2."const" para valores fixos//    
const TAXAS_JUROS = 0.05;        
TAXA_JUROS =0.10; ERRO! const não pode mudar        

//3.Exibindo no console do navegador (F12)        
console.log(nomeAluno); 
//-> "Maria"        
console.log(idade); 
//->21        
console.log(idade); 
//->21          */ 
                 
//---TIPOS PRIMITIVOS----------        

//1 String--texto, sempre entre aspas //       
let nome = "Ana"; 
//aspas duplas        
let cidade = 'São Paulo' 
//aspas simples        
let menssagem = `Olá, ${nome}!`; 
//template literal(moderno)        
console.log(mensagem);
//->"Olá, Ana!"  

//2 Number - números(inteiros e decimais)        
let pontos = 100;        
let temperatura = 36.7;        
let saldo = -50; //pode ser negativo 

//3. Boolean - verdadeiro ou Falso  
let estaLogado = true;        
let temDesconto = false;        
//4. UnderFined - varíavel declarada mas sem valor        
let resultado;        console.log(resultado);
//-> underfined        
//5. Null - ausência intencional de valor        
let usuarioAtivo = null;        
//Descobrir o tipo de qualquer valor        
console.log(typeof nome); 
//-> "String"        
console.log(typeof pontos); 
//-> "number"        
console.log(typeof estaLogado); 
//-> "boolean"    

//------------Operadores Aritiméticos ----------------//
let a = 10, b = 3;

    console.log (a + b);    // -> 13 (adição)
    console.log (a - b);    // -> 7  (subtração)
    onsole.log (a * b);    // ->30 (multiplicação)
    console.log (a / b);    // -> 3.33  (divisão)
    onsole.log (a % b);    // -> 1 (resto da divisão)
    console.log (a ** b);    // -> 7  (potência: 10²)
     
//------------Operadores de Comparação ----------------//
    console.log(5 === 5); // -> true (igual em valor E tipo)  
    console.log(5 !== 3); // -> true (diferente)
    console.log(10 > 8);  // -> true (maior que)
    console.log(5 <= b);  // -> true (menor ou igual)

    // Cuidado: == vs ===
    console.log(5 == "5");  // -> true (faz conversão automática)
    console.log(5 === "5"); // -> false (mais rigoroso, prefira este!)

    // ------Operadores lógicos -----------------------//
    console.log(true && false);  // -> false (E: ambos precisam ser true)
    console.log(true || false);  // -> true (Ou: basta um ser true)
    console.log(!true);          // -> false (Nâo: inverte o valor)

    //-----Concatenação de Stringsa---------------------------//
    let primeiroNome = "João";
    let sobrenome   ="Silva";
    let nomeCompleto = primeiroNome + "" + sobrenome;
    console.log(nomeCompleto);  // -> "João Silva"

    // Forma moderna com template literals
    console.log('Nome: ${primeiroNome} ${sobrenome}');  // -> "Nome: João Silva"

    // ------FORMA CLÁSSICA (Function Declaration)---------//
    function saudar(nome) {
        return 'Olá, ${nome}! Bem-vindo ao JavaScript!';
        }
        //Chamando a função
        let mensagem = sudar("Ana");
        console.log(menssagem); // -> "Olá, Ana! Bem-vindo so JavaScript!"
        console.log(saudar("Carlos"));  // -> "Olá, Carlos! Bem-vindo..."

        // ----FUNÇÃO COM MULTIPLOS PARÂMETROS ------------------//
        function calcularArea(largura, altura) {
            let area = largura * altura;
            return area;
            }

            console.log(calcularArea(5, 3));  // -> 15
            console.log(calcularArea(10, 4)); // -> 40

        //-----ARROW FUNCTION (forma moderna) ------------
        const dobrar = (numero) => numero 8 2;
        console.log(dobrar(7));  // -> 14
        
        // Arrow function com múltiplas linhas //
        const somar = (a, b) => {
            let resultado = a + b;
            return resultado;
        };
        console.log(somar(4, 6)); // -> 10

        //------PARÂMETROS PADRÃO --------------------//
        
        //---ESCOPO GLOBAL -------------------//
        let nome = "Global";   // <- visível em todo lugar
        
        let sobrenome = "Local";{  // <- só existe aqui dentro
        console.log(nome);        // -> "Global" (função acessa o externo)
        console.log(sobrenome);   // -> "Local"
    }

    minhaFuncao();
    console.log(mome);      // -> "Global"
    // console.log(sobrenome); X Erro! sobrenome não exite aqui

    // ----ESCOPO DE BLOCO (let e const)------------
    if (true) {
        let dentroDoIf = "s´aqui";
        console.log(dentroDoIf);    // -> "só aqui"
    }
    //  console.log(dentroDoIf); X Erro! fora do bloco




    </script>
    
</body>
</html>
