<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>


    <script>
        // Declarando variáveis - passo a passo
        // 1. "let" para valores que vão mudar
        let nomeAluno = "Maria";
        let idade = 20;
        idade = 20;
        idade = 21; //Posso alterar depois

        // 2. "const" para valores fixos
        const TAXA_JUROS = 0.05;
        // TAXA_JUROS = 0.10; X Erro! const não pode mudar

        // 3. Exibindo no console do navegador (F12)
        console.log(nomeAluno);  // -> "Maria"

        console.log(idade);  //-> 21
	 // -----TIPOS PRIMITIVOS----------------

        // 1. String - texto, sempre entre aspas
        let nome     = "Ana";   //aspa duplas
        let cidade   ='São Paulo'; //apas simples
        let mensagem = `Olá', ${nome}!`; //template literal (moderno)
        console.log(mensagem0); // -> "Olá, Ana"!

        // 2. Number - números (inteiros e decimais)
        let pontos      = 100;
        let temperatura = 36.7;
        let saldo       = -50;  //pode ser negativo

        // 3. Boolean - verdadeiro ou falso
        let estaLogado  = true;
        let temDesconto = false;
        
        // 4. Undefined - variável declarada mas sem valor
        let resultado;
        console.log(resultado);  //undefined

        //5. Null - ausência intencional de valor
        let usuarioAtivo = null;

        // Descobrir o tipo de qualquer valor
        console.log(typeof nome);        // -> "string"
        console.log(typeof pontos);     // -> "number"
        console.log(typeof estaLogado); // -> "boolean"
    </script>
      
</body>
</html>

    </script>
    
</body>
</html>
